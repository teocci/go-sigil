// Command sigil-mcp is the MCP server for the Sigil framework.
// It runs as a stdio transport server, serving symbol queries to AI agents.
package main

import (
	"fmt"
	"os"
)

func main() {
	fmt.Fprintln(os.Stderr, "sigil-mcp: not yet implemented — see Milestone 8")
	os.Exit(1)
}
